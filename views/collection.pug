extends layout.pug

block content

  h1 Metadata about the simple file server
  h2 Collections

  h3 #{json.title}
  h4 Links for the collection
    each link in json.links
      p #{link.rel} = 
        a(href= link.href)  #{link.title} 
        | (#{link.type})
  h2 Links
  each link in json.links
    p #{link.rel} = 
      a(href= link.href)  #{link.title} 
      | (#{link.type})
